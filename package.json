{
  "name": "safemap",
  "version": "1.0.0",
  "description": "SafeMap — Reportes de seguridad comunitaria",
  "dependencies": {
    "@capacitor-firebase/messaging": "^6.3.1",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "scripts": {
    "sync": "cap sync",
    "open": "cap open android",
    "build": "cap sync && cap open android"
  }
}
